import IBeerAPI from "../interfaces/IBeerAPI";

interface BeerProbs {
  beers: string;
  setBeers: React.Dispatch<React.SetStateAction<string>>;
}

function OutputBeer(props: BeerProbs) {
  return;
}

export default OutputBeer;
